spring:
  application:
    name: bank api
  datasource:
    url: jdbc:mysql://${MYSQLUSER}:${MYSQLPASSWORD}@${MYSQLPROXY}:${MYSQLPORTPUB}/${MYSQLDATABASE}
    username: ${MYSQLUSER}
    password: ${MYSQLPASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
